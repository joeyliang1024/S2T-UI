# 假日需求單

更新日期：2026-09-25

狀態：依目前程式碼完成靜態盤點後開始分階段實作。原有需求與執行順序保留，以下區分實作現況、已記錄的測試及待驗收項目。

## 1. 執行原則與順序

- **先完成 App.tsx 及其他必要重構，維持既有行為與外觀，再新增功能。**
- **重構完成後，優先完成 storage 等基礎建設，再接上註冊登入與後續產品功能。**
- 重構與介面改版分階段處理；先做既有功能回歸，再調整版面。
- 保留現行翻譯策略，新增可選策略，不直接取代。
- Web 與 Electron 均需支援；未配置遠端 storage 時提供本機測試模式。
- 資料儲存、查詢與權限需依使用者隔離。
- 現有雛形不直接視為已上線，需完成整合與驗收。

執行順序：

1. 整體重構與既有行為回歸。
2. Storage 基礎建設：儲存抽象層、資料結構／遷移、環境變數、MinIO／PostgreSQL／Milvus、本機 fallback、Web／Electron 存取及雙來源載入。
3. 註冊、登入、使用者隔離及術語庫，接入已完成的 storage 基礎。
4. 模型管理與 ASR 語言一致性。
5. 翻譯策略與語言自動偵測。
6. 聲紋註冊與混合語者辨識。
7. 即時收音設定、VAD、降噪與取樣率研究。
8. 歷史紀錄、編輯、接續錄音與匯出。
9. 摘要、會議整理與模板。
10. 介面改版、多國語系與主題。
11. 整合驗收、部署及操作文件。

### 目前進度

`App.tsx` 已拆為入口、`AppView` 與 `useAppController`，並有共用 service、auth 及 storage 模組；但主要畫面仍集中於 `AppView.tsx`，收音、翻譯、紀錄、摘要等流程仍集中於 `useAppController.ts`。完整重構尚未完成。

登入、storage adapter、模型管理、聲紋、歷史搜尋／排序／匯出及摘要已有不同程度的實作；左側歷史列表已接入主畫面。接續錄音與片段重講已有功能路徑與可切換版本，尚缺瀏覽器、Electron 與保存後重開的實機驗收。2026-09-25 已完成第一批 P0 修正：本機 storage 並行寫入及損毀檔處理、原子帳號建立、聲紋查詢使用者範圍、講者分離登入保護、工作階段遠端版本衝突保護，以及翻譯排隊、尾句收尾與版本回填保護。

本次已重新執行 `npm run storage:smoke`、`npm run typecheck`、`npm run build` 與 `git diff --check`；尚未執行模型 API、遠端儲存或麥克風實機驗收。既有 `[x]` 及日期表示已記錄的子項進度，不代表整項需求已完成；與目前程式碼不符的敘述已更正。

## 2. 完整需求清單

編號對應原始需求，方便逐項 Review。

| 編號 | 需求 | 預期結果 |
| --- | --- | --- |
| 1 | 混合語者分離 | 已註冊聲紋匹配後顯示真實姓名／NT，未匹配者自動分群；即時字幕可編輯講者。 |
| 2 | 註冊與登入 | 提供註冊、登入、登出；註冊填寫帳號、密碼、NT、Department；storage 資料依使用者隔離。 |
| 3 | 翻譯負載策略 | 保留現行策略，增加降低模型負載的可選策略。 |
| 4 | 自動偵測輸入語言 | 在設定的輸入／輸出語言範圍內偵測來源語言並產生對應翻譯。 |
| 5 | 翻譯時機 | 提供「即時」與「整句」翻譯策略。 |
| 6 | 摘要與會議整理 | 歷史紀錄可產生摘要或會議整理，套用或自訂 Markdown 模板，於 UI 顯示，並可選擇產出翻譯。 |
| 7 | 斷句與 VAD 滑桿 | 斷句速度、App VAD 等時間參數可用滑桿調整。 |
| 8 | 紀錄排序 | 依名稱、時間、時長排序，支援升冪／降冪。 |
| 9 | 複製逐字稿 | 可直接複製逐字稿文字。 |
| 10 | 儲存架構 | MinIO 放音檔、PostgreSQL 放文字資料、Milvus 放聲紋；環境變數配置、本機 fallback、Electron 本機／遠端保存與雙來源載入；定義 blob、config、vector 抽象層。 |
| 11 | 多國語系 | 繁中、簡中、英文、日文、德文。 |
| 12 | 深淺色模式 | 深色、淺色，並提供跟隨系統選項。 |
| 13 | 行話／術語庫 | 術語存於 PostgreSQL，可維護並套用至辨識與翻譯。 |
| 14 | 紀錄內搜尋 | 展開歷史紀錄後可用關鍵字搜尋。 |
| 15 | 語音降噪 | 增加可選擇的 denoising 處理。 |
| 16 | VTT 字幕 | 字幕檔改為 .vtt 格式。 |
| 17 | 匯出講者 | 講者資訊隨字幕一起匯出。 |
| 18 | 取樣率研究 | 研究並實測音訊取樣率對辨識、聲紋、音質、延遲及資源使用的影響。 |
| 19 | ASR 語言錯誤 | 修正語言選擇與 ASR 模型／實際請求不一致的問題。 |
| 20 | 接續歷史收音 | 歷史紀錄可繼續收音並更新音檔與逐字稿。 |
| 21 | 編輯時間段 | 可直接編輯時間段與內容，或對指定片段重講。 |
| 22 | 左側紀錄列表 | 左側顯示歷史紀錄，類似 OpenAI 對話紀錄列表。 |
| 23 | 選單與右側功能欄 | 右上角新增可展開選單，設定及其他分頁放入選單；右側欄選擇功能，主頁保留字幕。 |
| 24 | 右側即時設定 | 即時設定改由右側拉出，收音中可調整語言、音源、翻譯開關／語言、VAD 策略及間隔等。 |
| 25 | 優先重構 | App.tsx 拆為 view／hooks／service；其他必要重構一併先完成，保持行為與外觀。 |
| 26 | 模型註冊與列表 | 新增模型移至模型列表，可篩選；所有類別註冊需填 model name、endpoint、key、用途。 |
| 27 | 聲紋註冊 | 提供聲紋註冊與管理功能，供混合辨識使用。 |

### 2.1 逐項程式碼盤點（2026-09-25）

狀態定義：「已有實作／待驗收」表示主要路徑存在；「部分實作」表示仍有明確功能缺口；「待實作」表示未見對應流程。所有狀態均不等同正式上線簽核。

| 編號 | 現況 | 尚需完成／驗收 |
| --- | --- | --- |
| 1 | 部分實作：分群後會在目前使用者可見的已註冊聲紋範圍內比對並回填 NT；預設僅本人可見，擁有者可明確同意同部門或跨使用者使用；支援手動講者文字。 | 模型版本相容、已註冊講者選擇及混合多人實測；Electron 即時分離路徑亦需驗證。 |
| 2 | 部分實作：註冊／登入／登出／狀態恢復及首次 admin 初始化已存在；本機帳號建立已採原子檢查。Electron 打包版的 renderer 會透過 `S2T_GATEWAY_URL` 呼叫外部 HTTPS gateway；主程序已建立以 gateway 驗證後身分分隔帳號目錄的 IPC 邊界。 | 將登入／登出與主程序身分同步完整串接，完成音檔、逐字稿、設定、金鑰與復原檔的換帳號隔離測試；匿名資料歸屬遷移及打包後登入驗證。 |
| 3 | 部分實作：現有逐段請求、重試及單一有界翻譯工作者；可切換自動排隊或手動逐段翻譯；可取消目前 Web 請求及已排隊翻譯，過期結果不會回填。 | 合併批次與負載比較。 |
| 4 | 部分實作：ASR 自動偵測時省略 language；字幕文字會以中／英／日／德字元規則做候選來源標記，翻譯請求會使用該結果。 | 模型端 ASR 偵測語言回傳、限定配對／範圍及雙向翻譯；文字規則僅是 fallback，不能視為 ASR 偵測驗收。 |
| 5 | 部分實作：可選即時／整句，依標點或 VAD 邊界觸發；翻譯回填會驗證字幕 revision，停止時會收尾自動翻譯的 final 尾句再保存。 | 最大等待與實機 API 驗收。 |
| 6 | 部分實作：預設／自訂模板、輸出語言、附加翻譯、分段摘要、結果保存／複製／下載已存在；模板可另存、切換及刪除，歷史頁會以安全的基本 Markdown 顯示摘要，並以逐字稿 signature 標示摘要是否因後續編輯而過期。 | 長紀錄合併的上下文上限、失敗重試及重新開啟驗收。 |
| 7 | 部分實作：VAD 參數已有滑桿及數值；HTTP ASR 收音中調整會在下一段音訊套用。 | 真實噪音環境與避免切句首尾的實機驗收。 |
| 8 | 已有實作／待驗收：名稱、建立時間、時長及升降冪。 | 搜尋、分頁、同值排序與空資料回歸。 |
| 9 | 已有實作／待驗收：匯出側欄可選擇複製時是否包含時間、講者與翻譯；歷史紀錄複製會沿用同一選項，並保留剪貼簿失敗提示。 | 確認搜尋後複製範圍與瀏覽器／Electron 剪貼簿實機驗收。 |
| 10 | 部分實作：本機與三種遠端 adapter、環境變數及使用者範圍的 config／blob 路徑已存在；本機並行寫入、損毀檔保護及遠端 session 版本衝突保護已完成。 | 遠端實測、Milvus 初始化／重啟、音檔失敗恢復及 Electron 整合。 |
| 11 | 部分實作：五語系選擇與導覽字典；登入／註冊入口會依瀏覽器語言顯示五語系的標題、帳密欄位、提交與帳號隔離說明。 | 主工作區的按鈕、表單、提示、錯誤與無障礙文字多仍固定繁中，尚未完整五語系化。 |
| 12 | 部分實作：深／淺／系統切換、偏好保存及 light CSS；歷史側欄、模型卡、聲紋卡、設定內層、摘要與對話框已加入淺色覆寫。 | 浮動字幕、長文字與各平台實機可讀性驗收。 |
| 13 | 部分實作：術語逐筆新增／編輯／刪除／搜尋、登入保護保存 API、ASR／翻譯 prompt 串接；保存會檢查非成功 HTTP 回應。啟用 PostgreSQL 時以 `s2t_glossaries` 保存每位使用者的術語內容、版本與更新時間；前端保存會帶版本 compare-and-swap，衝突不會靜默覆寫，並可一鍵重新載入伺服器版本。 | 多人／遠端實測。 |
| 14 | 已有實作／待驗收：歷史展開後搜尋原文、譯文及講者。 | 空查詢、無結果、編輯後更新與長紀錄效能回歸。 |
| 15 | 部分實作：使用瀏覽器 noiseSuppression 約束；設定頁明示僅新建／切換麥克風 stream 生效，保存及 ASR 使用同一處理後 stream。 | 確認裝置是否實際支援；尚無獨立降噪模型處理。 |
| 16 | 部分實作：可匯出 WebVTT，會略過無效時間段並跳脫文字與講者標籤。 | 相容播放器實測。 |
| 17 | 已有實作／待驗收：VTT、JSON、CSV、複製與純文字逐字稿都包含講者。 | 匯出檔及保存工作階段回歸。 |
| 18 | 部分實作：已有研究／測試建議文件及聲紋 16 kHz 重採樣。 | 尚無可重現的品質、延遲、資源與取樣率實測結果。 |
| 19 | 部分實作：開始收音與匯入會傳入來源語言；auto 省略 language；HTTP ASR 收音中變更會在下一段音訊套用。模型能力契約已包含支援語言與取樣率，開始收音前會檢查來源語言與實際 AudioContext 取樣率。 | 模型 API／實機能力資料驗收與匯入路徑取樣率檢查。 |
| 20 | 部分實作：歷史紀錄可接續收音；既有字幕保留，新字幕會依原始時長偏移，停止後會將新音訊接到目前選定版本並建立新的可切換版本。 | 瀏覽器與 Electron 的接續錄音、保存／重開／播放／匯出實機驗收，以及接續失敗復原流程。 |
| 21 | 部分實作：即時及歷史字幕均可編輯文字、翻譯、講者與時間；可直接以麥克風或上傳 WAV 重講歷史片段，系統會以原始時間範圍替換音訊、建立可切換的新版本並保留所有舊版本；重講成功後會以目前 ASR 模型回填該片段文字，ASR 失敗則保留原字幕；播放／WAV 匯出使用目前選定版本。 | 瀏覽器／Electron 實機時間軸、ASR 與版本切換驗收。 |
| 22 | 部分實作：主畫面新增常駐左側歷史列表，點選可開啟對應歷史逐字稿；窄視窗改為橫向清單。 | 大量歷史的虛擬化／分頁與實機動線驗收。 |
| 23 | 部分實作：右上選單、功能分組、路由、左側歷史及 live 工具側欄已有。 | 右側工具切換與窄視窗；頁面切換不干擾收音的回歸。 |
| 24 | 部分實作：live 快速設定浮層可拖曳；音源可由收音面板切換；HTTP ASR 的來源語言、術語及 VAD 可在收音中於下一段生效。 | 模型切換、WebSocket adapter、右側入口與實際音訊缺口的驗收。 |
| 25 | 部分實作：入口與部分 feature／service 已拆分。 | 大型 view／controller、model adapter、gateway 路由及 Electron main 繼續依責任拆分，補齊回歸基線。 |
| 26 | 部分實作：模型列表、四類用途、註冊／編輯對話框、用途篩選及名稱／ID／endpoint 搜尋；Electron ASR 註冊與編輯都可安全保存 key；已可設定 ASR 模式、VAD、時間戳精度、支援語言及支援取樣率，收音前會依能力檢查。 | 聲紋用途、明確免 key 模式、Web 後端模型註冊與實際請求模型一致性。 |
| 27 | 部分實作：錄製／上傳／註冊／列表／刪除已存在；註冊前會檢查 WAV 可解碼、至少 3 秒、至多 90 秒且非近似靜音；列表會顯示 embedding 模型／版本，舊資料會明示需重新註冊。 | 多講者／噪音品質判定、使用者範圍比對、遠端刪除及權限回歸。 |

## 3. 第一階段：整體重構

對應需求：25，並涵蓋新增功能前必要的其他重構。

| 範圍 | 工作內容 |
| --- | --- |
| App.tsx | 保留應用程式組裝與入口，不承擔所有畫面及業務流程。 |
| Views／Components | 分離即時字幕、歷史紀錄、匯入、模型列表、設定、浮動字幕與共用元件。 |
| Hooks | 分離收音生命週期、裝置切換、字幕狀態、翻譯佇列、紀錄管理及模型設定。 |
| Services | 分離音訊編碼、模型呼叫、逐字稿匯出、設定讀寫及儲存操作。 |
| Model Adapter | 分離 WebSocket、HTTP ASR、翻譯及能力描述，整理取消、錯誤與回應處理契約。 |
| Web Gateway | 拆分路由、設定、模型代理與服務邏輯，建立登入及 storage 接入邊界。 |
| Electron Main／Preload | 分離錄音寫檔、工作階段、模型金鑰、IPC 與浮動視窗。 |
| 共用契約 | 統一工作階段、字幕片段、講者、模型、設定與錯誤型別，定義資料版本及遷移方式。 |

驗收條件：

- [x] Web／Electron 建置及型別檢查通過（2026-09-24）。
- [x] App 入口、feature、shared service 與 server storage／auth 已依責任重整至資料夾（2026-09-24）。
- [ ] 原畫面結構、樣式及操作流程保持不變。
- [ ] 開始、暫停、繼續、停止、切換音源及尾段保存維持既有行為。
- [ ] 翻譯、講者分離、匯入、歷史播放、匯出及浮動字幕回歸通過。
- [ ] 原有資料與金鑰保留，讀取與保存路徑不因拆分失效。
- [ ] 對音訊時間軸、佇列及資料遷移等高風險邏輯建立必要驗證。

## 4. 第二階段：Storage 基礎建設（重構後最高優先）

對應需求：10；先建立第 2、13、27 項所需的資料基礎。

本階段優先交付可驗證的儲存能力，註冊登入畫面與其他產品功能在下一階段接入。

建置順序：

1. 定義 blob、config、vector 介面、文字資料存取層及資料版本。
2. 建立 schema／遷移與使用者歸屬關係；使用者 ID 及聲紋主鍵對應從一開始納入設計。
3. 完成本機實作，讓未配置遠端服務時可測試。
4. 完成環境變數載入、配置驗證及 MinIO／PostgreSQL／Milvus adapter。
5. 接入 Web 後端與 Electron 主程序，完成本機／遠端保存及雙來源合併載入。
6. 驗證資料遷移、連線失敗、保存失敗與恢復流程，補齊基礎建設操作文件。

儲存介面自始要求使用者範圍；本階段以測試身分驗證資料歸屬，下一階段接入後端登入驗證後才開放多使用者存取。

### 4.1 儲存責任與抽象層

| 資料 | 遠端實作 | 抽象層／資料存取責任 |
| --- | --- | --- |
| 音檔及二進位物件 | MinIO | blob：寫入、讀取、刪除、物件資訊。 |
| 設定及文字資料 | PostgreSQL | config 及文字資料存取層：使用者、設定、紀錄、字幕、摘要、模板、術語。 |
| 聲紋向量 | Milvus | vector：新增、查詢、比對、刪除。 |

- 每種服務獨立配置；未配置時使用對應的本機實作。
- 已配置但缺少必要欄位或連線失敗時明確報錯，不靜默切換儲存目的地。
- Web 經後端存取 storage；本機測試模式的資料由後端保存。
- Electron 可選擇保存至本機或遠端，載入時須同時讀取兩個來源。
- 使用穩定 ID、版本資訊及來源標示合併紀錄，避免重複顯示或靜默覆寫。
- 遠端保存失敗時清楚標示未同步／未保存狀態。
- 連線憑證只由 Web 後端或 Electron 主程序讀取，不暴露給 renderer。

### 4.2 Milvus 欄位限制

Milvus 的業務欄位只保存：

| 欄位 | 說明 |
| --- | --- |
| NT | 使用者顯示名稱。 |
| Department | 使用者部門。 |
| embedding | 聲紋向量。 |

- 另保留 Milvus 必要的技術主鍵。
- 不存帳號、密碼、音檔、逐字稿或其他業務欄位。
- 使用者 ID 與聲紋主鍵的歸屬關係、embedding 模型版本及其他管理資訊由 PostgreSQL 保存。
- 聲紋存取權限與允許的查詢範圍由後端控制，不能僅憑前端傳入 NT／Department 決定。

### 4.3 Storage 環境變數

| 服務 | 設定項目 | 環境變數 |
| --- | --- | --- |
| MinIO | endpoint | `S2T_MINIO_ENDPOINT` |
| MinIO | bucket | `S2T_MINIO_BUCKET` |
| MinIO | access key | `S2T_MINIO_ACCESS_KEY` |
| MinIO | secret key | `S2T_MINIO_SECRET_KEY` |
| PostgreSQL | host | `S2T_POSTGRES_HOST` |
| PostgreSQL | port | `S2T_POSTGRES_PORT` |
| PostgreSQL | db name | `S2T_POSTGRES_DB_NAME` |
| PostgreSQL | user | `S2T_POSTGRES_USER` |
| PostgreSQL | password | `S2T_POSTGRES_PASSWORD` |
| Milvus | endpoint | `S2T_MILVUS_ENDPOINT` |
| Milvus | db name | `S2T_MILVUS_DB_NAME` |
| Milvus | collection | `S2T_MILVUS_COLLECTION` |
| Milvus | token | `S2T_MILVUS_TOKEN` |
| Electron 打包版 gateway | 外部 HTTPS gateway base URL | `S2T_GATEWAY_URL` |

`S2T_GATEWAY_URL` 僅供 Electron 主程序讀取；打包版未設定時不得回退至內嵌或相對 `/api`。開發與本機簡易測試可使用相對路徑或明確指定 HTTP localhost gateway。

驗收條件：

- [x] 未配置遠端服務時可使用本機模式完成操作；blob、config、vector smoke test 已通過（2026-09-24）。
- [x] `npm run storage:smoke` 以隔離暫存目錄檢查 config／blob 分使用者讀寫、並行 config 寫入、session compare-and-swap 衝突、原子帳號建立、聲紋候選範圍、刪除與損毀 vector 檔錯誤（2026-09-25）。
- [ ] 三種遠端儲存均可依環境變數正常運作。
- [ ] Milvus 僅含指定業務欄位及必要技術主鍵。
- [ ] Electron 合併載入本機與遠端紀錄，無重複或靜默覆寫。
- [ ] 舊資料遷移、儲存失敗及恢復流程可驗證。
- [x] blob、config、vector 及文字資料存取層可透過一致契約使用本機／遠端實作；本機 blob／config／vector 讀寫與向量查詢 smoke test 已通過（2026-09-24）。
- [x] PostgreSQL 基礎 schema 包含使用者及聲紋主鍵對應表（原記錄：2026-09-24）；目前聲紋 API 寫入的是 config records，尚未接上專用對應表與模型版本管理，不代表歸屬／版本流程已驗收。
- [x] Web 已透過登入保護 API 保存及載入工作階段與 WAV，並完成跨使用者工作階段／音檔隔離 smoke test（2026-09-24）。

## 5. 第三階段：註冊、登入、使用者隔離與術語庫

對應需求：2、13；依賴第二階段 storage 基礎建設完成。

### 5.1 註冊與登入規格

註冊必填：

| 欄位 | 用途 |
| --- | --- |
| 帳號 | 登入識別，必須唯一。 |
| 密碼 | 登入驗證；僅保存安全雜湊，不保存明文。 |
| NT | username display，使用者顯示名稱。 |
| Department | 使用者部門。 |

- 提供註冊、登入、登出與登入狀態恢復。
- 系統首次啟動時建立管理員帳號：帳號 `admin`、密碼 `admin`、角色 `admin`；僅在帳號不存在時初始化，不覆寫既有帳號。
- PostgreSQL 保存穩定的使用者 ID、帳號、密碼雜湊、NT、Department。
- 以使用者 ID 判斷資料歸屬，不以可變動的顯示名稱作為權限依據。
- 後端驗證資料擁有者；隔離範圍包含音檔、字幕、紀錄、摘要、模板、設定、術語與聲紋存取。
- 本機測試模式同樣需有註冊、登入與使用者資料隔離能力。
- Electron 打包版先以 access token 向 `S2T_GATEWAY_URL/api/auth/session` 驗證帳號，主程序只以驗證回傳的穩定 user ID 決定帳號資料目錄；renderer 傳入的 user ID 不可作為授權依據。
- Electron 本機帳號資料目錄為 `<userData>/accounts/<verified-user-id>/`，其中保存模型設定、加密 API key 與可復原錄音；工作階段音檔與逐字稿也須以相同身分範圍管理。
- 規劃既有匿名本機資料的遷移及首次帳號歸屬流程。

### 5.2 行話／術語庫

- 支援新增、編輯、刪除、搜尋及套用術語。
- 儲存於 PostgreSQL；本機模式經相同資料存取介面處理。
- 可供 ASR 提示及翻譯使用，遵守使用者隔離。

驗收條件：

- [x] 可註冊、登入、登出；重複帳號及錯誤密碼有明確提示。已完成本機 auth smoke test 與 renderer 建置驗證（2026-09-24）。
- [x] 使用者不能讀取、修改或刪除其他使用者的工作階段與音檔；已完成跨使用者隔離 smoke test（2026-09-24）。
- [x] 登入身分與 Web storage 使用者範圍由後端 JWT 串接，前端不能指定其他使用者 ID（2026-09-24）。
- [x] 本機 gateway 的可重跑權限 smoke 已驗證：未登入 sessions／聲紋／分離 API 會拒絕，兩個帳號無法跨讀工作階段或 WAV（`npm run gateway:auth-smoke`，2026-09-25）。
- [ ] 遠端 gateway 與打包版 Electron 完成註冊、登入及權限驗證。
- [ ] 術語可維護、保存及套用至辨識／翻譯。

## 6. 第四階段：模型管理與 ASR 語言一致性

對應需求：19、26。

- 將新增模型移到模型列表，支援用途篩選及搜尋。
- 所有類別使用一致表單：model name、endpoint、key、用途。
- 本機無驗證服務可明確選擇不使用 key。
- 用途涵蓋 ASR、翻譯、摘要、講者分離、聲紋辨識；降噪模型依實作需要納入。
- 模型能力描述包含支援語言、串流能力、時間戳精度與取樣率要求。
- 修正語言選單、模型設定及實際 ASR 請求不一致的問題。
- 切換模型時檢查選定語言是否支援。
- API key 使用 Electron 安全保存或後端保存，不寫入一般前端設定。

驗收條件：

- [ ] 各用途模型皆可註冊、查看及篩選。
- [x] 選定語言正確傳至即時及匯入 ASR 路徑；來源／目標限定中文、英文、日文、德文，自動偵測時省略 ASR language 欄位（2026-09-24）。
- [ ] 不支援的模型／語言組合有明確提示。

## 7. 第五階段：翻譯策略與自動偵測

對應需求：3、4、5。

| 設定 | 選項 |
| --- | --- |
| 觸發時機 | 即時、整句。 |
| 負載策略 | 保留現行策略；新增合併批次、手動翻譯。 |
| 來源語言 | 固定語言、自動偵測。 |
| 翻譯方向 | 固定目標語言、限定語言配對的雙向翻譯。 |

- 自動偵測限制在設定的輸入／輸出語言範圍內，優先使用 ASR 提供的語言資訊。
- 整句策略依標點與停頓累積，設最大等待時間，停止收音時處理剩餘內容。
- 日文整句翻譯以 `。！？` 或 App VAD 自然停頓任一者作為句尾；在句尾前合併字幕片段，降低逐段翻譯造成的語序錯誤。
- 合併批次及有界佇列降低模型請求負載，不阻塞 ASR 與錄音保存。
- 手動翻譯模式不自動送出字幕；使用者可逐段按「翻譯此段」，以在模型負載高時控制請求量。
- 支援失敗重試、取消及過期結果檢查，避免舊結果覆蓋人工編輯。
- 顯示目前策略、待翻譯及失敗狀態。

驗收條件：

- [x] 現行即時逐段與完整句子策略皆可選用；完整句子依標點或 VAD 停頓觸發（2026-09-24）。
- [ ] 相同測試音訊下，可比較新策略的請求數、等待時間及翻譯完整度。
- [ ] 語言範圍內的雙向輸入可產生正確方向的翻譯。
- [x] 翻譯 Web 請求有 15 秒逾時與重試；失敗只標記該字幕，不停止收音或 ASR（2026-09-25）。
- [x] 停止收音時，會將自動翻譯模式下尚未翻譯的 final 尾句排入既有單一佇列，待完成後才建立工作階段快照；revision／原文不符的舊結果不會回填（2026-09-25；待實機 API 驗收）。

## 8. 第六階段：聲紋註冊與混合語者辨識

對應需求：1、27。

- 提供聲紋註冊頁，支援錄製或上傳樣本、品質檢查、註冊及刪除。
- 聲紋關聯 NT 與 Department；模型、版本與共享範圍存於 PostgreSQL，不擴充 Milvus 業務欄位。預設僅本人可用；使用者選擇同部門或跨使用者範圍後，**必須勾選同意**才會註冊為共享聲紋。
- 先做語者分群，再比對已註冊聲紋。
- 達匹配門檻顯示真實姓名／NT；未匹配者保留穩定匿名講者編號。
- 本機 sherpa-onnx 已有 PCM16 WAV → embedding、註冊、比對與刪除路徑；原文件記錄本機 smoke test 通過（2026-09-24）。2026-09-25 靜態核對：獨立聲紋路由有登入驗證，但比對搜尋未限使用者，混合辨識的 `/api/diarizations` 亦未要求登入；因此權限驗收仍未完成。
- [x] 聲紋註冊、管理與混合語者回填已串接：可直接收音或上傳 WAV 樣本；分群後會以每位講者的合併音訊比對聲紋，命中顯示 NT，未命中保留 `SPEAKER_XX`；即時字幕講者欄可自由編輯（2026-09-24）。
- 即時字幕可指定已註冊講者、修改講者或重新命名匿名講者。
- 人工指定結果不被後續自動辨識直接覆蓋。
- 明確處理不同 embedding 模型或版本間的不相容。

驗收條件：

- [ ] 已註冊與未知講者混合對話可正確匹配／分群。
- [ ] 不同未知講者不會全部被當成同一人。
- [x] 即時與歷史逐字稿皆可人工修正講者；人工名稱保存後不會被後續自動分離覆蓋（2026-09-25）。
- [ ] 註冊、刪除、比對均通過權限驗證。

## 9. 第七階段：收音、VAD、降噪與取樣率

對應需求：7、15、18、24。

- 斷句間隔、最短語音、靜音等待、前置保留及相關 VAD 參數使用滑桿，顯示單位、範圍與目前值。
- 即時設定從右側拉出，提供語言、音源、翻譯開關／語言、VAD 策略及間隔設定。
- 定義收音中設定立即生效或下一段生效，避免重建流程造成音訊缺口。
- 增加可開關的降噪，明確定義保存音訊及送模型音訊是否經處理。
- 實測不同取樣率對 ASR、聲紋、降噪、頻寬、延遲及資源使用的影響。
- 依模型要求執行真正的重採樣，不只修改 WAV header。
- 根據測試結果決定降噪與取樣率預設值。

驗收條件：

- [ ] 收音中可調整指定參數，生效時機清楚且不丟失音訊；HTTP ASR 的來源語言、術語及 VAD 已於下一段生效，仍需實機驗收。
- [x] 右側即時設定已有翻譯開關、目標語言、策略及停頓斷句控制；完整設定的 App VAD 參數使用滑桿（介面實作，原記錄：2026-09-24）。
- [x] HTTP ASR adapter 已提供收音中設定更新：來源語言、術語與 VAD 在下一段音訊生效，不重建 capture pipeline（2026-09-25；待實機驗收）。模型切換及 WebSocket adapter 不在此契約內。翻譯設定與音源切換另依各自流程驗證。
- [ ] 安靜、背景噪音、短停頓及長句情境下，VAD 不明顯切掉句首／句尾。
- [ ] 比較降噪前後 ASR 與聲紋結果，記錄品質影響。
- [ ] 提交取樣率比較結果及建議，時間戳與音訊時長保持一致。
- [x] 已建立取樣率／降噪策略與建議實測項目的文件，建議裝置原生錄音及聲紋 16 kHz 真正重採樣；**尚未提交實測數據，不能視為研究驗收完成**（2026-09-25）。

## 10. 第八階段：歷史紀錄、編輯與匯出

對應需求：8、9、14、16、17、20、21。

- 名稱、時間、時長排序，支援升降冪。
- 展開紀錄後可搜尋原文、譯文及講者。
- 複製逐字稿，可選是否包含時間、講者及翻譯。
- 字幕匯出改為 WebVTT（.vtt），保留講者資訊；其他逐字稿格式也保留講者。
- 歷史紀錄可接續收音，新增音訊與字幕接續原有時間軸。
- 可直接編輯片段文字及時間範圍。
- 可對指定片段重講，保留原音檔及各重講版本並可切換；版本切換不得破壞前後音訊及時間軸。
- 編輯結果同步反映於保存、搜尋、複製及匯出。

驗收條件：

- [ ] 排序及搜尋結果正確，分頁與排序一致。
- [x] 歷史紀錄可依名稱、時間、時長升降冪排序，支援整筆與展開後逐字稿的原文、譯文、講者搜尋（2026-09-24）。
- [x] 逐字稿複製使用目前可見／指定的字幕片段，包含講者與翻譯文字（2026-09-25）。
- [x] 可複製包含講者的逐字稿，並可輸出包含講者標籤的 WebVTT、JSON、CSV（2026-09-24）。
- [ ] VTT 可由相容播放器讀取，時間戳、講者標籤及特殊字元正確。
- [ ] 接續錄音後保存、重開、播放及匯出皆完整。
- [ ] 片段編輯／重講後，字幕與音訊同步且其他片段不受破壞。

## 11. 第九階段：摘要、會議整理與模板

對應需求：6。

- 歷史紀錄可產生摘要或會議整理。
- 提供預設模板，並允許自訂、保存及套用 Markdown 模板。
- 可選輸出語言，以及是否另產出翻譯版本。
- UI 渲染 Markdown，處理不可信 HTML，提供複製及匯出。
- 長逐字稿分段整理後合併，控制模型上下文與請求負載。
- 保存模板、來源紀錄版本與生成結果，方便編輯後重新生成。

驗收條件：

- [x] 預設與自訂模板皆可生成並顯示 Markdown 結果；模板可另存、切換與刪除，且模板資料會隨設定保存（2026-09-25；待 UI 實機驗收）。
- [x] 摘要／會議整理可套用預設或自訂 Markdown 模板，選擇中英日德輸出與是否另產出翻譯；結果保存於歷史紀錄並可重新生成（2026-09-25）。
- [ ] 可選擇是否輸出翻譯及其語言。
- [ ] 長紀錄可完成整理，失敗時有提示及重試途徑。
- [x] 長逐字稿以 24,000 字元附近的自然斷點分段整理；中間摘要也會依相同上限分層合併，最後才套用模板。供應商回傳異常長的中間摘要時會停止重複合併，待模型 API 驗收其上下文控制與錯誤提示（2026-09-25）。
- [ ] 結果可保存、重新開啟、複製與匯出。

## 12. 第十階段：介面改版、多國語系與主題

對應需求：11、12、22、23；整合第 24 項即時設定。

- 左側顯示歷史紀錄，類似對話紀錄列表。
- 主區域保留字幕。
- 右上角新增可展開選單，收納設定、模型、聲紋、術語、匯入等入口。
- 選單切換需同步更新 URL 路徑（`/live`、`/history`、`/import`、`/models`、`/voiceprints`、`/settings`），支援重新整理、瀏覽器前進／返回及直接開啟連結；右上角三線按鈕僅控制右側選單展開狀態。
- 右側功能欄選擇要開啟的工具，即時設定由右側拉出。
- 多國語系：繁體中文、簡體中文、英文、日文、德文。
- 翻譯範圍包含畫面、提示、錯誤及無障礙標籤。
- 支援淺色、深色、跟隨系統，保存使用者偏好。
- 驗證窄視窗、浮動字幕、長文字及不同語系的版面。

驗收條件：

- [ ] 新版左右側欄及右上選單符合上述動線，主要字幕可持續使用；目前已實作左側歷史、右上選單與 live 右側工具，待實機驗收。
- [x] 右上角三線按鈕可開啟右側功能選單，工作區與管理功能已分組；Web 切換同步 `/live`、`/history`、`/import`、`/models`、`/settings` 路徑，並支援返回與直接開啟（2026-09-25）。
- [x] 完整設定依「轉錄與 VAD、翻譯與術語、摘要整理、講者分離、應用程式」分類，避免單頁堆疊所有選項（2026-09-25）。
- [x] 模型列表提供遮罩式 ASR 模型註冊；聲紋管理拆為 `/voiceprints` 獨立功能，快速設定限定於 `/live` 並可拖曳調整位置（2026-09-25）。
- [ ] 五種語系可切換且沒有遺漏主要操作文字。
- [ ] 深淺色模式與浮動字幕皆可正常閱讀。
- [ ] 切換頁面、工具或主題不干擾正在進行的收音。

## 13. 第十一階段：整合驗收與上線

- [x] 已依程式碼逐項盤點 27 項需求的實作與缺口，列於第 2.1 節（2026-09-25；本次僅文件更新）。
- [ ] 補齊每項需求的可重跑測試證據及 Web／Electron 實機驗收結果；靜態盤點不取代驗收。
- [ ] 驗證跨使用者隔離、本機／遠端載入、舊資料遷移及服務斷線處理。
- [ ] 驗證長時間錄音、翻譯背壓、停止尾段及歷史接續錄音。
- [ ] 實測已知／未知講者混合對話、多語翻譯、降噪與不同取樣率。
- [ ] 完成 Web／Electron 的建置、操作回歸及資料相容性驗證。
- [ ] 補齊環境變數範例、部署、備份還原及驗收文件。
- [x] 已補齊本機／遠端 Storage 環境變數、啟動、備份、還原與隔離檢查文件（2026-09-25）。

## 14. Review 時需定案的細節

以下不改變已列需求，用於避免實作時默認不明確的產品規則：

| 項目 | 待定案內容 |
| --- | --- |
| NT 與 Department | NT 是否全域唯一；Department 為自由輸入或固定選單；兩者是否允許修改。 |
| 講者真實姓名 | 是否直接以 NT 顯示，或需要額外的講者姓名欄位（額外欄位不存 Milvus）。 |
| 聲紋註冊及使用範圍 | 已定案：預設僅本人；擁有者選擇同部門或跨使用者範圍時，必須勾選同意。是否允許替他人註冊聲紋仍待定。 |
| 自動翻譯語言範圍 | 第一版支援的來源／目標語言清單，以及雙向翻譯時的具體配對。介面五語系不等同模型支援語言。 |
| 歷史時間排序 | 預設依建立時間；是否另需最後更新時間。 |
| 片段重講 | 已定案：保留原始與重講版本供切換；仍須以實機驗收確認時長變動後的時間軸規則。 |
| Electron 雙來源衝突 | 同一紀錄本機與遠端皆有修改時的處理方式，避免靜默覆寫。 |
| 正式上線環境 | 已定案：Electron 只連外部 HTTPS gateway，不啟動或內嵌 gateway；本機服務只供簡易測試。實際網域、TLS 憑證、三種 storage 與模型服務驗收環境待部署時提供。 |


## 15. 本次 Review 發現與後續工作

以下是目前程式碼的靜態觀察及待辦。已完成項目會保留完成狀態，未完成項目仍依「重構與回歸 → storage → 登入與隔離 → 產品功能」推進；基礎資料完整性與隔離問題應在對外多使用者使用前完成。

### 15.1 優先處理的基礎缺口

| 優先序 | 問題及程式證據 | 待辦與驗收 |
| --- | --- | --- |
| P0（部分完成，待 API 實測） | `server/index.cjs`：聲紋比對與本機混合分離要求登入；註冊預設私有，使用者明確選擇後才會供同部門或全體登入使用者比對，且只比對相同 embedding 模型／版本。 | 補 API 層未登入、私有／同部門／跨部門授權、刪除及未知講者測試；Electron 即時路徑仍需驗收。 |
| P0（已完成，待故障測試） | `server/storage/local.cjs`：config／vector 讀改寫已序列化；只在檔案不存在時初始化，損毀檔案會報錯。`LocalUserStore` 改用原子建立。 | 補權限／磁碟寫入失敗與跨程序鎖定策略；現有 smoke 已覆蓋並行、重複帳號及格式損毀。 |
| P0（已完成，待 UI 實測） | `useAppController.ts`：本機與遠端同時載入，遠端 session 採 version compare-and-swap；同 ID 不同內容會保留遠端衝突版本。 | 補多視窗、離線重連、使用者可見的衝突處理與音檔同步驗收。 |
| P0（已完成，待故障測試） | `useAppController.ts` 的 `stopCapture`：本機及遠端音檔各自嘗試保存；兩者都失敗時會下載復原 WAV，歷史紀錄會明示音檔未保存，不再覆蓋錯誤為成功訊息。 | 服務斷線、配額不足及重新同步實機驗收。 |
| P0（部分完成） | `src/main/index.ts`：打包版由 `S2T_GATEWAY_URL` 指向外部 HTTPS gateway，renderer 的 auth、storage、轉錄、翻譯及摘要 API 會使用此位址；未設定 gateway 的打包版會拒絕 API 存取。本機開發可保留相對 URL 供簡易測試。登入／登出已同步主程序的 gateway 驗證身分，模型設定、加密金鑰、復原錄音及保存的工作階段會寫入該帳號目錄；主程序音檔讀取及開啟工作階段會核對擁有者。復原 manifest 的讀改寫已按帳號序列化並使用唯一暫存檔，避免同時 IPC 寫入衝突。 | 補打包版的換帳號保存／開啟驗收與舊外部工作階段遷移策略。開發 Vite proxy 成功不能取代打包驗收。 |
| P1（已完成，待遠端實測） | `server/storage/remote.cjs`：Milvus 第一次建立 collection 時會建立 cosine AUTOINDEX；既有 collection 會核對 `id`、`NT`、`Department`、embedding 型別與維度。刪除前會檢查 collection 是否存在，不再依程序記憶體的 dimension 決定略過。 | 驗證「服務重啟後未先搜尋就刪除」真的刪除向量，並在正式 Milvus 版本確認 index 與 SDK 回應。 |
| P1（部分完成） | 聲紋註冊資料已保存 embedding 模型／版本；比對只會使用目前模型與版本完全相同的資料，缺少 metadata 的舊資料需重新註冊。啟用 PostgreSQL 時，`s2t_voiceprint_records` 會實際保存 vector ID、擁有者、模型與版本，刪除時驗證擁有者並同步移除。若 config 保存失敗，會回滾本次新增的向量及 metadata。 | 補舊資料遷移、遠端服務故障實測與模型相容性策略。 |

### 15.2 產品功能的明確缺口

- **翻譯**（`useAppController.ts`）：已有單一序列佇列、revision／原文檢查及停止收音的尾句 flush；人工修改或使用者取消後的舊結果不會回填。Web 可中止當前請求，Electron 送出後的回應只會被忽略。合併批次、負載比較與實機 API 驗收仍待補齊。
- **即時設定**（controller、`features/models/model-adapter.ts`）：HTTP ASR 已於下一段套用來源語言、術語與 VAD，且不重建收音；模型切換與 WebSocket adapter 尚未支援收音中更新。需驗證設定更新不造成音訊缺口或時間軸重置。
- **模型管理**（`AppView.tsx`、`src/main/index.ts`、gateway）：Electron ASR 編輯流程已保存新輸入的 key；Web API 使用環境模型，列表選擇尚未形成完整後端模型註冊／路由契約；ASR／文字服務仍要求 key。需補上免 key 的明確選擇、每模型憑證綁定、支援語言／取樣率及聲紋用途，不可僅保存前端表單。
- **歷史與匯出**（controller、`shared/services/transcript.ts`）：歷史展開可改文字、翻譯、講者及時間；可接續收音，並可直接錄製或上傳 WAV 重講片段。兩者都建立可切換音檔版本並保留原版；重講成功會回填該片段 ASR，失敗保留原字幕。WebVTT 已跳脫文字、限制講者標籤並略過無效時間，但仍需以相容播放器驗證 cue 解析。
- **術語**：設定頁已提供逐筆新增／編輯／刪除／搜尋，並保存為使用者隔離的術語文字資料；保存會檢查 HTTP 狀態。PostgreSQL 專用術語 schema、術語版本與 ASR／翻譯實機套用仍待驗收。
- **摘要**：模板可另存、切換及刪除；摘要保存來源逐字稿 signature，歷史頁會安全顯示基本 Markdown 並提示後續編輯造成的過期。仍需分段合併的上下文限制、失敗重試及重新開啟驗收。
- **介面**（`shared/i18n/index.ts`、`AppView.tsx`、`styles.css`）：導覽五語系及主題切換只是基礎；左側紀錄列表已接入主畫面。仍需補齊主要文案／錯誤／無障礙標籤，以及不同主題與大量紀錄的畫面驗收。
- **音訊實測與效能**：降噪只使用瀏覽器約束，需記錄實際支援情況。Web PCM 仍累積於記憶體，定時分離重建全段 WAV；Electron PCM 寫入後不進同一陣列，因此不可直接認定兩端即時分離都已完成。長時間錄音、資源使用及取樣率品質比較均待實測。

### 15.3 驗證證據與文件整理

| 類別 | 目前證據 | 後續補件 |
| --- | --- | --- |
| 建置與型別 | 2026-09-25 已通過 `npm run typecheck`、`npm run build` 與 `git diff --check`。 | 後續程式變更後重跑同一組檢查。 |
| 本機 storage | 2026-09-25 `scripts/storage-smoke-test.cjs` 已通過；覆蓋 config／blob／vector、並行寫入、重複帳號、損毀檔與 session CAS。 | 遷移、載入競態、保存失敗與聲紋 API 隔離測試。 |
| 登入／隔離／聲紋 | 2026-09-25 `npm run gateway:auth-smoke` 通過未登入拒絕、Alice／Bob 工作階段與 WAV 隔離，以及 Vite 與 Electron `file://`（`Origin: null`）的聲紋刪除 CORS preflight；桌面帳號目錄由 TypeScript／建置檢查覆蓋。 | 補 Electron 打包版換帳號保存／開啟、聲紋共享範圍 API 與模型品質驗收。 |
| 遠端服務 | 三種 adapter 已存在；本次未連線驗證。 | 在驗收環境記錄 MinIO、PostgreSQL、Milvus 的初始化、重啟、讀寫刪除、隔離及斷線結果，不在文件記錄憑證。 |
| Web／Electron | 程式路徑及既有 `docs/VALIDATION.zh-TW.md`。 | 瀏覽器、Electron 開發版與打包版分開驗收；麥克風、系統音訊、浮動字幕及長時間錄音需實機。 |
| 需求文件一致性 | `TODO.md` 仍有舊路徑及「唯一待辦來源」敘述，驗收文件部分保存／字幕格式描述也與目前實作不同。 | 本次以本需求單作為 27 項需求 Review 清單；後續同步整理 TODO、驗收與架構文件。本次不修改其他文件。 |

### 15.4 尚待定案事項

已定案：片段重講保留可切換的原始與重講版本；聲紋可供同部門或跨使用者比對，但必須由聲紋擁有者在註冊時明確同意，預設為僅本人。登入帳號切換時，桌面程式的音檔、逐字稿、設定、API 金鑰與復原檔都必須完全隔離。正式服務一律連外部 HTTPS gateway；Electron 不內嵌或啟動 gateway，本機服務只可作為簡易測試。打包版以 `S2T_GATEWAY_URL` 指定外部 gateway，並須在主程序以已驗證的登入身分建立本機資料隔離邊界，不能信任 renderer 傳入的 user ID。

## 16. UI 快照盤點與企業級視覺改版計劃

本節只記錄探索、設計目標與驗收規則；未授權在此階段修改 UI。2026-09-25 已以 Electron 開發版取得登入頁快照，並依 `AppView.tsx`、`AuthGate.tsx` 與 `styles.css` 盤點所有可到達介面。開發版在未配置 gateway 時可使用本機簡易測試路徑；正式版快照必須另以外部 HTTPS gateway 驗收。

| 介面 | 現況盤點 | 視覺改版目標 |
| --- | --- | --- |
| 登入／註冊 (可以暫時使用即可, 之後會換成專門的 TSSO) | 深色單一卡片、帳密表單、錯誤提示與帳號隔離文案。 | 建立可信賴的企業登入入口：清楚的產品識別、登入狀態、密碼錯誤與服務連線層級；保留高對比與鍵盤焦點。 |
| 即時收音 | 主字幕工作區、左側歷史、快速設定、匯出側欄與收音控制共存。 | 以「收音狀態 → 即時字幕 → 操作」形成固定閱讀順序；讓錄音、模型、來源語言、翻譯狀態與錯誤可一眼辨識。 |
| 歷史紀錄 | 左側近期清單、排序／搜尋、展開逐字稿與多個操作按鈕。 | 建立密度可控的企業資料列表、清楚的選取狀態與空白狀態；將高風險刪除與日常檢視分開。 |
| 匯入與摘要 | 上傳、轉錄結果、模板與摘要結果位於功能頁／側欄。 | 將輸入、處理中、成功、需重試與輸出結果明確分層，避免大型表單與次要設定搶走主要流程。 |
| 模型管理 | 模型卡、用途篩選、註冊／編輯對話框與能力設定。 | 使用一致的資料表／卡片規格、模型健康狀態、用途標籤與安全憑證提示，使複雜設定可掃讀且可審核。 |
| 聲紋管理 | 錄音儀表、WAV 上傳、分享同意、已註冊聲紋清單。 | 把「錄製／品質／同意分享／註冊完成」做成可理解的逐步流程；以低調插畫或抽象音波點綴，不把裝飾誤認為資料或權限狀態。 |
| 完整設定與即時設定 | 分類設定頁與可拖曳快速設定浮層。 | 建立一致的設定分組、說明層級、保存狀態與變更生效時機；快速設定只保留收音中真正需要的項目。 |
| 浮動字幕 | 深色透明字幕視窗、文字與全螢幕控制。 | 在各種背景上維持可讀性，提供字級、對比、位置與無障礙偏好；控制項在非互動時保持低干擾。 |

### 16.1 設計系統與美術方向

- 以成熟的 B2B 音訊工作台為方向：深淺色皆使用穩定的中性色基底、單一主色、語意色與清楚的狀態層級；不使用會降低資訊辨識的過量漸層、裝飾圖示或動態。
- 建立 token：色彩、字級、間距、圓角、陰影、焦點、表格密度、圖示尺寸與狀態 badge 必須共用，避免頁面各自定義視覺語言。
- 美術只用在登入、空白狀態、聲紋註冊與摘要完成等非關鍵資料區，採抽象聲波、頻譜、語句節奏等主題；不得以生成圖遮蓋字幕、表單、錯誤或權限資訊。
- 以可存取性為完成條件：文字和互動元件需有足夠對比、可見焦點、鍵盤操作、螢幕閱讀器名稱；動畫需可降低或關閉。

### 16.2 執行順序與驗收

1. 先建立所有頁面的現況截圖基線：登入、註冊、即時收音、歷史空／有資料、匯入、模型、聲紋、設定及浮動字幕；每頁至少記錄深色、淺色與窄視窗狀態。
2. 先完成資訊架構、設計 token、字級和互動狀態，再改動單頁元件；不在視覺改版中改變登入、錄音、翻譯、儲存或權限邏輯。
3. 依使用頻率改版：登入與即時收音 → 歷史與匯出 → 模型／聲紋／設定 → 匯入／摘要／浮動字幕。
4. 每個頁面用相同資料與操作流程比較改版前後截圖；確認長逐字稿、錯誤、載入、空白、成功與權限受限狀態都不溢位或失焦。
5. 完成 Web、Electron 開發版及打包版的深淺色、窄視窗與鍵盤回歸；正式版另確認外部 HTTPS gateway 錯誤能以產品文案呈現，不暴露技術堆疊。

驗收條件：所有功能頁有可重製的前後快照；即時收音頁在 1280px 寬度與窄視窗仍可清楚讀取字幕和操作；所有狀態可用色彩以外的文字或圖示辨識；登入、權限、服務故障與刪除確認不顯示原始技術錯誤；美術資產均有來源、授權與深淺色對比檢查紀錄。
