# 新版需求單：未完成與待驗收項目

更新日期：2026-09-26

依據：`假日需求單.md`、目前工作目錄程式碼、`docs/VALIDATION.zh-TW.md`，以及後續對話確認的介面需求。本文件只保留剩餘工作；已完成且已有對應驗收證據的子項不重列。

本次已重新執行本機驗證：`storage:smoke`、`gateway:auth-smoke`、`resample:smoke`、`glossary:smoke`、`model-adapter:smoke`、`typecheck`、production `build` 與 `git diff --check` 均通過。本機儲存／gateway adapter 是目前 MinIO、PostgreSQL、Milvus 的簡易測試替代層；外部服務部署不列為此階段的阻擋項目。

狀態定義：

- **未完成**：仍有功能缺口、已辨識問題，或需要釐清並修正的程式路徑。
- **待驗收**：已有實作，但尚缺對應情境的可重現驗收結果。
- 項目完成後，從本清單移除；驗收證據另存驗收文件，記錄版本、平台、步驟與結果。

## 一、優先補完

### N01 全介面語言切換 — 未完成（原需求 11）

- 補齊五語系：繁中、簡中、英文、日文、德文。涵蓋工作區、設定、模型表單、摘要、術語、聲紋、歷史操作清單、空狀態、錯誤訊息、提示與無障礙標籤。
- `i18n:smoke` 已驗證主選單、通用介面與登入字典的每個已登錄 key 均具五語系文字；仍需逐頁清除固定文案並以快照驗收。
- `AppView.tsx`、`useAppController.ts` 仍存在大量固定中文；日期及狀態判斷也需避免依賴固定顯示文字。
- 驗收：逐頁切換語言，所有系統文案立即更新；重新開啟保留偏好，使用者輸入的模板／紀錄內容不被改寫。

### N02 淺色模式與主題完整性 — 未完成（原需求 12）

- 清查 `styles.css` 的固定深色值、重複覆寫與選擇器優先順序，補齊尚未跟隨主題的元件。
- 範圍包含下拉選單、浮層、對話框、字幕、摘要、術語、模型卡、選取／停用／錯誤狀態及浮動字幕視窗。
- 驗收：深色、淺色、跟隨系統逐頁快照；系統主題變更即時生效，文字、邊框、焦點及選取狀態均清楚可辨。

### N03 自動取樣率處理的實機驗收 — 待驗收（原需求 18、19、26）

- 即時 ASR 已依模型目標率選擇、重取樣；時間清除邊界已改用送模型的取樣率。串流跨區塊一致性、48k→16k、44.1k→16k、原率直通已由 `resample:smoke` 覆蓋。
- 仍需以真實模型與裝置確認停止時尾端、字幕時間戳與長時間收音品質。
- 模型支援取樣率由使用者／服務端明確宣告；未填能力會維持未知並使用裝置原始取樣率，不把預設列表當成服務保證。仍待服務端能力探測契約。
- 檢查目前註冊／編輯入口是否能設定能力，並讓即時、匯入、片段重講皆遵守相同取樣率契約。
- 驗收：48k→16k、44.1k→16k、原率直通、不同區塊長度及長時間收音；核對輸出樣本數、音訊時長、字幕時間戳、清除／接續後邊界與實際 API 接受格式。保存音訊與送模型音訊的取樣率須各自正確。

### N04 模型設定與實際服務一致 — 未完成（原需求 19、26）

- 補齊 Web 模型註冊／選擇與 gateway 請求路由契約，確認實際使用使用者所選 endpoint、model ID 與能力，而非僅更換前端選項。
- Electron ASR 已將「不需要 API key」隨受控 IPC 傳到主程序，並在設定檔保留；chunked adapter 會略過 key 檢查，主程序則以不帶 `Authorization` 的 multipart 請求呼叫，避免自架服務因假憑證遭拒。仍需補齊 Web gateway／其他模型用途的同等契約與實際呼叫驗收。
- 驗收：有 key／免 key 模型的新增、修改、重開與呼叫；不支援語言／取樣率有明確處理；一般前端設定與輸出不得包含明文 key。

## 二、仍需完成的產品與整合工作

### N05 翻譯策略與自動語言 — 待驗收（原需求 3、4、5）

- 已實作連續 HTTP final 合併、220 ms 聚合窗口、逐句模式 5 秒最長等待及既有策略切換。
- `translation-policy:smoke` 已覆蓋連續 HTTP 段合併、即時模式、標點／句界與 5 秒上限，以及失敗段不自動重送。
- 已優先接收 ASR 偵測語言，字元規則僅在服務未回傳時作為限定範圍的後備。
- 驗收：相同音訊比較請求數、延遲、完整度；日文標點／停頓、無標點長句、停止尾句、取消、失敗重試與編輯後晚到回應均符合預期。

### N06 摘要流程與長內容可靠性 — 待驗收（原需求 6）

- 摘要頁已以每筆紀錄 generation 防止重複產生覆蓋；失敗會清除暫存結果，不能匯出失敗文字。
- 長逐字稿已分段／分層合併；若服務未縮短內容會明確中止，不會悄悄只保留第一組。
- `summary-plan:smoke` 已驗證逐字稿切分可無遺失重組、分層批次邊界及內容簽章變更偵測。
- 來源逐字稿變更時，摘要頁與歷史卡片已顯示重新產生提示；待操作驗收歷史 `…` 進入獨立摘要工作區、模板／語言設定後產生、格式化、複製、匯出、重新產生與失敗重試。

### N07 自訂模板立即保存與管理 — 待驗收（原需求 6）

- 「自訂＋ → 保存」已直接寫入使用者設定，待以重開應用程式驗收跨重開保存。
- `summary-templates:smoke` 已覆蓋模板名稱、空內容預設格式、立即選取與至少保留一個模板；管理頁只提供預覽及刪除。仍待實機驗收保存失敗提示與跨重開保存。
- 驗收：左側固定高度捲動清單，選項高度固定；下方刪除按鈕與清單同寬。右側預覽的框頂對齊清單框頂、框底對齊刪除按鈕底，說明文字獨立、不占對齊範圍；不重複顯示灰色「已保存模板」。長模板、很多模板及窄視窗仍可操作。

### N08 術語 JSON 匯入與返回動線 — 待驗收／部分未完成（原需求 13）

- JSON 匯入已抽為純解析模組，`glossary:smoke` 已覆蓋物件／陣列格式、錯誤格式、無效資料、重複項目及空內容；UI 也限制 1 MB。仍待人工驗收大型檔案的可用提示與實際匯入操作。
- Electron 本機模式的術語保存已不依賴 gateway，會跟隨帳號設定保存；待驗收匯入後逐筆編輯／搜尋／刪除、重開與帳號隔離，以及 ASR prompt／翻譯實際套用。
- 「返回上一頁」已保留來源工作區（包含摘要、匯入、模型與聲紋）；待以各入口操作驗收。不要恢復舊的「重新載入伺服器術語」按鈕。
- PostgreSQL／MinIO／Milvus 目前以本機替代層完成保存、隔離與基本衝突流程驗收；外部服務整合只列入上線前驗收，不能阻擋本階段完成。

### N10 Electron 打包與帳號隔離 — 待驗收（原需求 2、10）

- 以真正打包版驗證登入、登出、逾期身分及缺少 gateway 設定時的行為；目前可先以本機 gateway 與本機儲存替代層驗證授權及帳號隔離。外部 HTTPS gateway 為上線前整合驗收。
- A／B 帳號切換後，模型設定、金鑰、模板、術語、字幕、摘要、音檔、工作階段與錄音復原均不能跨帳號存取。
- 驗收保存、重開、音檔播放及舊資料遷移；Vite proxy 或開發版成功不能代替打包版結果。

### N11 聲紋共享與即時講者辨識 — 未完成／待驗收（原需求 1、27）

- 補 Electron 即時音訊送分離的路徑檢查：目前桌面錄音寫入磁碟，不能直接依賴 Web 累積的 PCM 陣列。
- 非私有分享現在由前端在未勾選同意時停用註冊，Gateway 也強制請求包含明確同意 header；`gateway:auth-smoke` 已通過。仍待以實際 API 驗收未登入、私有、同部門、跨部門、跨使用者及刪除權限。
- 實測已註冊與多名未知講者混合對話、雜訊、不同 embedding 模型／版本，以及人工名稱不被自動結果覆蓋。

### N12 收音設定、降噪與資源使用 — 未完成／待驗收（原需求 7、15、18、24）

- 收音期間已鎖定 ASR 模型選擇與模型編輯／刪除；畫面會說明模型在開始收音時固定套用。仍待操作驗收 WebSocket 與模型切換邊界。
- 實測 HTTP 設定於下一段生效、音源切換、VAD 句首句尾保留、降噪開關及瀏覽器實際支援情況。
- Web 長時間 PCM 累積與反覆全段分離需量測並改善；提交取樣率／降噪對辨識、聲紋、延遲、頻寬、CPU／記憶體影響的可重現比較。

### N13 結構整理與文件一致性 — 未完成（原需求 25）

- 已抽出術語 JSON、翻譯策略與摘要規劃的純模組及 smoke tests；繼續拆分大型 `AppView.tsx`、`useAppController.ts` 與 gateway，建立其餘必要的行為回歸檢查。
- 更新 TODO、架構及驗收文件中的舊路徑、保存方式與過時介面描述；舊右側快速設定／拖曳浮層不再作為新驗收目標。
- 驗收文件區分 Web、Electron 開發版與打包版，保留可重跑步驟與實際結果。

## 三、已有功能路徑，仍待驗收

以下只列缺少的驗收，不要求重新實作已有功能。

| 編號／原需求 | 待驗收範圍 | 通過條件 |
| --- | --- | --- |
| V01／8、14、22 | 歷史搜尋、排序、分頁與大量資料 | 搜尋原文／譯文／講者，編輯後更新；名稱／時間／時長升降冪及同值穩定性；側欄前 40 筆以外可經全部紀錄找到，空資料與大量紀錄可操作。 |
| V02／9、16、17 | 複製與匯出 | 歷史操作入口可到達，時間／講者／翻譯選項可設定；TXT、VTT、JSON、CSV 與目前版本一致，搜尋後輸出範圍明確，剪貼簿失敗有提示。即時收音頁不恢復匯出區。 |
| V03／20、21 | 接續、片段重講與版本 | Web／Electron 都能保存、重開、播放與匯出所選版本；舊版保留可切換，片段變長／變短時間軸正確，ASR 失敗與接續失敗可恢復。 |
| V04／10 | 同步與保存故障 | 多視窗修改、離線重連、版本衝突、音檔保存失敗及配額不足不靜默覆寫或假報成功；本機／遠端結果與復原 WAV 可確認。 |
| V05／22、23、24 | 側欄與字幕版面 | 只在即時字幕頁出現歷史側欄；收起呈現可辨識 icon，點 icon 直接展開該功能，展開以頂部選單切換，收起確實增加字幕寬度；功能區隨視窗高度捲動。搜尋在小 A 左側，小 A／大 A 並排，段數集中。快速設定齒輪與收合圖示符合提供的側欄參考。 |
| V06／22、23 | 歷史載入與 `…` 選單 | 舊紀錄可直接切換；即時內容尚未保存時先安全保存再載入，收音中的切換限制清楚。操作清單具 icon、可關閉、鍵盤操作與焦點管理，小視窗不被裁切。 |
| V07／26 | 模型對話框 | 註冊按鈕置中，用途與其他欄位同寬、下拉一致；只有點既有 model card 進入才顯示刪除，空白註冊或切換用途不出現刪除；低視窗可捲動操作。 |
| V08／6 | 摘要與模板重開 | 自訂模板、選擇、刪除與摘要結果跨重開保存；複製／Markdown 匯出與畫面一致，重新產生不混入其他紀錄結果。先完成 N06、N07。 |
| V09／7、15、18 | 實機錄音與復原 | 麥克風、系統音訊、混音、暫停／繼續、停止尾音、浮動字幕、60 分鐘錄音、睡眠喚醒、低磁碟與強制關閉復原；分別記錄 Web、macOS／Windows Electron 的結果。 |
| V10／19、26 | 匯入與串流協定 | 大型 WAV 分段／重疊去重、取消、MP3／M4A 路徑及能力限制；WebSocket partial、認證與重連須有服務端契約和實測。 |
| V11／11、12、23 | UI 全頁快照與可用性 | 登入、字幕、歷史、摘要、匯入、模型、聲紋、設定及對話框，補深淺色／不同高度／長內容／錯誤狀態快照；鍵盤焦點、文字對比與企業級視覺一致。先完成 N01、N02。 |

## 四、仍待確認的條件

- **帳號資料規則**：NT 是否全域唯一、Department 自由輸入或固定清單、兩者是否可修改；講者是否直接顯示 NT，是否允許代他人註冊聲紋。
- **自動翻譯範圍**：正式支援的雙向語言配對；不可把五種 UI 語言直接當成模型能力。
- **歷史與衝突規則**：是否需要最後更新時間排序；本機與遠端皆修改時的使用者選擇流程。
- **外部部署環境**：實際 HTTPS gateway、TLS、三種儲存服務及模型服務配置。這是上線前驗收，不是目前本機功能測試的前置條件；憑證不寫入需求單。

## 五、後續執行順序

1. 依使用者優先要求完成全元件語系與主題（N01、N02），同時優先排除取樣率／時間軸正確性問題（N03）。
2. 完成模型實際路由、摘要／模板保存與術語返回動線（N04、N06～N08）。
3. 補翻譯、聲紋及錄音可靠性（N05、N11～N12）。外部服務於上線前以同一套 adapter 契約驗收。
4. 逐項執行第三節驗收，必要重構與文件同步隨對應功能完成；有缺陷就保留在本單，通過且有證據才移除。
